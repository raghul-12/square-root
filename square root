#include <stdio.h>
#include <math.h>

int main() {
 double x = 64;
 double y = 125;

 double x1 = sqrt(x);
 double y1 = sqrt(y);
 printf("Square root of %.2f is %.2f\n", x, x1);
 printf("Square root of %.2f is %.2f\n", y, y1);
}
